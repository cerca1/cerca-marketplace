export const metadata = {
  title: "Cerca — Comprá y vendé cerca tuyo",
  description: "Marketplace local de Firmat y la zona"
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
